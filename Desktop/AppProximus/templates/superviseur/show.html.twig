{% extends 'base.html.twig' %}

{% block title %}Superviseur{% endblock %}

{% block body %}
    <h1>Superviseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ superviseur.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ superviseur.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ superviseur.roles ? superviseur.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ superviseur.password }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ superviseur.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ superviseur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ superviseur.prenom }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ superviseur.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('superviseur_index') }}">back to list</a>

    <a href="{{ path('superviseur_edit', {'id': superviseur.id}) }}">edit</a>

    {{ include('superviseur/_delete_form.html.twig') }}
{% endblock %}
